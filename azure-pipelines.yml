trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'Azure subscription 1(b7628aa8-705c-40f0-bbee-a9bde27419ec)'
    appType: 'webAppLinux'
    WebAppName: 'ludo-capital-api'
    deployToSlotOrASE: true
    ResourceGroupName: 'ludo_capital'
    SlotName: 'production'
    packageForLinux: '$(System.DefaultWorkingDirectory)/InnovationInABox-CI/drop/s/build'

- task: PublishBuildArtifacts@1
